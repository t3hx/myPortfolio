dist
pnpm-lock.yaml
public/draco

# Les artefacts de la session design sont figés : les maquettes documentent des
# captures, et le style compact de tokens.css (plusieurs déclarations par ligne)
# est celui dans lequel le design system a été écrit et relu. Les reformater les
# ferait diverger de ce que docs/DESIGN.md décrit.
#
# L'exclusion SUIT le fichier (#111) : tokens.css est du code servi au visiteur,
# il vit donc dans src/ — mais il reste écrit dans le style de la session. Sans
# la seconde ligne, `pnpm format` en réécrit les 46 ko d'un coup.
design
src/styles/tokens.css

# Le plan du lab (#141) : 2 346 tracés SVG, une donnée générée, jamais
# éditée à la main. Reformatée par Prettier, elle devient illisible en diff.
src/content/labPlan.json
